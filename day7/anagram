  unordered_map<char,int>map;
    
    for(int i=0;a[i]!='\0';i++)
    map[a[i]]++;
    for(int i=0;b[i]!='\0';i++)
    if(map[b[i]]>0){
    map[b[i]]--;
    else
     return false;
    }
    return true;
