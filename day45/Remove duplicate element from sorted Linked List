Node *removeDuplicates(Node *head)
{
    Node *ptr=head;
    while(ptr->next != NULL){
        if(ptr->data==ptr->next->data)
        ptr->next=ptr->next->next;
    
    else
    
        ptr=ptr->next;
    }
    
    return head;
 // your code goes here
}
