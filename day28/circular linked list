bool isCircular(Node *head)
{
    Node *ptr=head;
    Node *node=head;
    while(ptr !=NULL){
        ptr=ptr->next;
        if(ptr==node){
            return 1;
        }
    }
    return 0;
   // Your code here
}
