string reverseWord(string str){
    int n=str.size();
    int j=n-1;
    char temp;
    for(int i=0;i<n/2;i++)
    {
        temp = str[i];
        str[i]=str[j];
        str[j]=temp;
        j--;
    }
    return str;
    
  //Your code here
}
